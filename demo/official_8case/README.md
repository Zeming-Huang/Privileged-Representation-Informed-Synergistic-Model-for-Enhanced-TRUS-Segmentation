# Official 8-Case Demo Data

Placeholder for the externally hosted 8-case demo package used for public PRISM reproduction.

The public `inference.py` script expects the same-distribution slice-level `channel_0/imgs/*.npy` and `channel_0/gts/*.npy` format, plus case-level `.npz` files for evaluation.
