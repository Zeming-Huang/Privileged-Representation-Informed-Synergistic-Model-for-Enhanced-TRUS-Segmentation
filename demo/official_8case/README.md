# Official 8-Case Demo Data

This folder is a placeholder for the externally hosted 8-case demo package used for public PRISM reproduction.

After download, the package should contain:

- `channel_0/imgs/*.npy`
- `channel_0/gts/*.npy`
- `channel_0/*.npz`

The public `inference.py` script expects this same-distribution slice-level format.
